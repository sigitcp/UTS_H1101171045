<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uts_h1101171045">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".aktivitas"></activity>
        <activity android:name=".servis"></activity>
        <activity android:name=".broadcast"></activity>
        <activity android:name=".konten"></activity>
        <activity android:name=".intent"></activity>
        <activity android:name=".synaktivitas"></activity>
        <activity android:name=".synservis"></activity>
        <activity android:name=".synbroadcast"></activity>
        <activity android:name=".synkonten"></activity>
        <activity android:name=".synintent"></activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>